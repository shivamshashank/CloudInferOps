apiVersion: v1
kind: Service
metadata:
  name: ollama-service
  namespace: inference
  labels:
    app: ollama
spec:
  ports:
  - port: 11434
    targetPort: 11434
    protocol: TCP
    name: api
  selector:
    app: ollama
  type: ClusterIP
